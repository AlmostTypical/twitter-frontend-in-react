const React = require('react');

const WhoToFollow = React.createClass({
  render: function () {
    return(
      <div className=" WhoToFollow profileCard panel">
        <div className="panel-heading has-text-centered ">
          <h2>Who to Follow</h2>
        </div>
      </div>
    )
  }
});

module.exports = WhoToFollow